#!/usr/bin/env bash

# exit immediately if any command fails
set -e 

bin/release                   
mix compile                   

bin/export                    
git add .                   
git commit -am"chore: export to HTML" 

echo "----- PUSH TO GITHUB"  
git push --follow-tags
