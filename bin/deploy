#!/usr/bin/env bash

# exit immediately if any command fails
set -e 

bin/release                   
mix compile                   
bin/export                    

echo "----- PUSH TO GITHUB"  
git add .                   
git commit -am"chore: deploy" 
git push --follow-tags
